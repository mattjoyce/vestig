# Core dependencies
python-sqlite-graph>=0.1.0  # Graph database with Python bindings

# LLM and embeddings
llm>=0.13.0                 # Simon Willison's LLM CLI tool
sentence-transformers>=2.3.0  # For BGE-m3 embeddings
transformers>=4.36.0        # Hugging Face transformers

# Embeddings model (BGE-m3)
# Will be downloaded on first use via sentence-transformers
# Model: BAAI/bge-m3 (1024-dim)

# Graph and data processing
networkx>=3.0               # For PageRank and graph algorithms
numpy>=1.24.0              # Numerical operations
scipy>=1.10.0              # For mutual information calculations

# NLP utilities
scikit-learn>=1.3.0        # For k-means clustering, Jaccard similarity
nltk>=3.8.0                # For keyword extraction (optional)

# CLI and configuration
click>=8.1.0               # CLI framework
pyyaml>=6.0                # YAML config parsing
python-dotenv>=1.0.0       # Environment variables

# Date/time handling
python-dateutil>=2.8.2     # For relative date parsing
pytz>=2023.3               # Timezone support

# Optional: GPU acceleration
# torch>=2.0.0             # PyTorch for GPU (uncomment if using GPU)

# Development tools (optional)
# pytest>=7.4.0            # Testing framework (when we add tests later)
# black>=23.0.0            # Code formatting
# ruff>=0.1.0              # Fast linting
anthropic>=0.39.0
